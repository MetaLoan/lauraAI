# LauraAI 生产就绪清单

## ✅ 已完成的核心功能

### 1. 积分系统 (Points System)
- [x] 后端数据库添加 `points` 和 `lra_balance` 字段
- [x] 创建 `/api/users/me/points/sync` 接口用于同步聊天积分
- [x] 创建 `/api/users/me/points/harvest` 接口用于结算积分为 LRA
- [x] 前端 `apiClient.syncPoints()` 连接到真实后端
- [x] 前端 `apiClient.claimLRA()` 连接到真实后端
- [x] 移除 localStorage 模拟,使用数据库持久化

### 2. DeFi 数据抓取 (Market Intelligence)
- [x] 创建 `DeFiHandler` 处理器
- [x] 实现 `/api/market/intelligence` 接口
- [x] 集成 Binance API 获取真实 BNB 价格
- [x] 前端 `apiClient.getMarketIntelligence()` 连接到真实后端
- [x] 移除前端随机模拟数据

### 3. 余额校准 (Balance Sync)
- [x] `getUserProfile()` 直接从后端 `/users/me` 获取数据
- [x] Profile 页面显示真实的 `points` 和 `lra_balance`
- [x] Portfolio 卡片显示真实的积分和余额
- [x] 移除所有 localStorage 余额模拟

## 📋 数据库迁移

```bash
# 执行迁移脚本
cd backend
psql -U your_user -d lauraai < migrations/add_user_balance.sql
```

或者让 GORM 自动迁移:
```go
// 在 repository/db.go 的 InitDB() 中
db.AutoMigrate(&model.User{})
```

## 🚀 启动服务

### 方式一: 使用统一脚本
```bash
./start-full-stack.sh
```

### 方式二: 分别启动
```bash
# Terminal 1: 后端
cd backend
./server

# Terminal 2: 前端
npm run dev
```

## 🔍 验证清单

### 1. 积分同步测试
- [ ] 在聊天页面发送消息
- [ ] 检查后端日志确认 `/api/users/me/points/sync` 被调用
- [ ] 在 Profile 页面查看积分是否增加
- [ ] 刷新页面,积分应该持久化

### 2. 积分结算测试
- [ ] 在 Dashboard 点击 "Harvest LRA"
- [ ] 检查后端日志确认 `/api/users/me/points/harvest` 被调用
- [ ] 积分清零,LRA 余额增加
- [ ] 刷新页面,余额应该持久化

### 3. 市场数据测试
- [ ] 打开 DeFi Analytics 组件
- [ ] 检查是否显示真实的 BNB 价格
- [ ] 检查后端日志确认调用了 Binance API
- [ ] 价格应该是实时的,不是固定值

### 4. 余额显示测试
- [ ] Profile 页面显示的 Points 和 LRA Balance 来自数据库
- [ ] Dashboard Portfolio 卡片显示的数据来自数据库
- [ ] 多个标签页打开,数据应该一致

## 🐛 已知问题

1. **GORM 事务导入**: `user.go` 中使用了 `repository.DB.Transaction`,需要确保导入了 `gorm.io/gorm`
2. **数据库迁移**: 需要手动执行 SQL 迁移或使用 AutoMigrate
3. **Binance API 限流**: 如果请求过于频繁,可能被限流,建议添加缓存

## 📝 下一步建议

1. **添加缓存层**: 使用 Redis 缓存市场数据,减少 API 调用
2. **积分规则配置化**: 将 +5 LRA/消息 改为可配置的规则
3. **交易历史**: 记录每次积分同步和结算的历史
4. **实时通知**: 当积分到账时推送通知给用户
5. **防作弊机制**: 限制单位时间内的积分获取上限

## 🎯 生产环境部署前

- [ ] 配置生产数据库连接
- [ ] 设置环境变量 (API Keys, Database URL)
- [ ] 启用 HTTPS
- [ ] 配置 CORS 白名单
- [ ] 添加 Rate Limiting
- [ ] 设置日志收集和监控
- [ ] 备份数据库
- [ ] 压力测试

---

**当前状态**: 核心功能已完成,可以进行本地测试。生产部署前需要完成上述验证清单。
