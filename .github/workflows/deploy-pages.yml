name: 部署到 GitHub Pages

on:
  push:
    branches: [ main, DESKTOP-BSC ]
  workflow_dispatch:

# 设置 GITHUB_TOKEN 权限
permissions:
  contents: read
  pages: write
  id-token: write

# 允许一个并发部署
concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    name: 构建项目
    runs-on: ubuntu-latest
    
    steps:
      - name: 检出代码
        uses: actions/checkout@v4
      
      - name: 设置 Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      
      - name: 安装依赖
        run: npm ci
      
      - name: 运行代码检查
        run: npm run lint || true
        continue-on-error: true
      
      - name: 构建项目
        run: npm run build
        env:
          # 前端部署固定使用 fly.io 后端，不依赖 secrets
          NEXT_PUBLIC_API_URL: 'https://soulface-backend.fly.dev/api'
          NEXT_PUBLIC_RPC_URL: 'https://eth.llamarpc.com'
          NEXT_PUBLIC_LAURA_AI_SOULMATE_ADDRESS: '0x9c2D6F27EC4F8438964Bb2e6540b4c8C735796DE'
          NEXT_PUBLIC_LAURA_AI_MARKETPLACE_ADDRESS: '0x798FA4D90E77C57dc9Bb390ACFe58b09dad9110e'
          NEXT_PUBLIC_LAURA_AI_STAKING_ADDRESS: '0xF089354e049990cceE4916A9D7B0f8eE96277d70'
          # WalletConnect project id is required for mobile wallet deep-link flow.
          NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID: '8131eee5b7cc58c87d6fb7e651722132'
          # basePath 由 next.config.mjs 根据 GITHUB_REPOSITORY 自动推导
          NEXT_OUTPUT_EXPORT: 'true'
      
      - name: 检查构建输出
        run: |
          if [ -d "out" ]; then
            echo "✅ 构建成功，out 目录已生成"
            du -sh out
            ls -la out
          else
            echo "❌ 构建失败，out 目录不存在"
            exit 1
          fi
      
      - name: 上传构建产物
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./out

  deploy:
    name: 部署到 GitHub Pages
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    
    steps:
      - name: 部署到 GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
